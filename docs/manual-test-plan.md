# 手动测试方案（v0.1.1 验收基线）

> 用途：在本地亲手验证已实现功能（FastAPI 后端 + Vue3 纯白主题前端 + 浏览器扩展 v1）。
> 测试对象：当前代码（macOS/Windows 打包版同样适用；打包版专项见 §4 Cookie 与 §5 下载；**浏览器扩展见 §11（X）**）。
> v0.1.1 新增：P2 批量上限（§2 U09/U10、§8 E06）、P3 历史持久化（§5.5 H）、P4 历史重试（§5.5 H）、P5 打包版内置浏览器（§4 C09）。
> v1 扩展新增：§11 浏览器扩展（X01–X12；设计见 `docs/design-extension-v1.md`）。
> 配套文档：`docs/design-analysis.md`（设计）、`docs/design-extension-v1.md`（扩展设计）、`docs/development-guide.md`（规范）。

> **验收记录 2026-09-05（v0.1.1 双平台）**：mac 启动/无感 Cookie/下载全链路通过；Windows 修复版（stdio 崩溃修复）启动与下载链路通过；下方 ✓ 为本次验收覆盖项（含 v0.1.1 专项）。

---

## 0. 测试准备

### 0.1 环境（已满足则跳过）
- [ ] `ffmpeg -version` 可执行（已装 9.0.1）
- [ ] `venv/bin/python --version` = 3.12
- [ ] `frontend/node_modules` 已安装

### 0.2 启动被测系统（二选一）

**方式 A：开发双服务**
```bash
# 终端 1
cd backend && ../venv/bin/uvicorn app.main:app --reload --port 8000
# 终端 2
cd frontend && npm run dev
# 打开 http://localhost:5173
```

**方式 B：生产单端口**
```bash
cd frontend && npm run build   # 若 dist 未构建
cd backend && ../venv/bin/uvicorn app.main:app --port 8000
# 打开 http://127.0.0.1:8000
```

### 0.3 测试数据
| 形态 | 链接 |
|------|------|
| 标准（带尾斜杠） | `https://www.bilibili.com/video/BV1hk4y1W76R/` |
| 标准（无尾斜杠） | `https://www.bilibili.com/video/BV1Z8h36gEnp` |
| 裸 BV | `BV1JRuA6vEvd` |

### 0.4 测试状态位
- 每用例记：**通过 ✓ / 失败 ✗ / 阻塞 ⚠（说明）**
- 下载类用例需**有效 Cookie**（见 §4 Cookie 用例）；无 Cookie 时 §5 标 ⚠。

### 0.5 扩展测试准备（§11 用例前必做）
- [ ] `cd extension && npm install && npm test && npm run build`（纯函数单测全绿 + dist 产出）
- [ ] `chrome://extensions` 开启「开发者模式」→「加载已解压的扩展程序」→ 选择 `extension/` 目录（含 manifest.json）
- [ ] 准备一个**已登录** B 站视频页标签页（登录态判定仅看 SESSDATA cookie 是否存在）

---

## 1. 冒烟测试（SF）

| # | 操作 | 预期 | 结果 |
|---|------|------|------|
| SF01 | 打开页面 | 纯白页面 + 任务/输入/设置面板；无 3D 场景（three.js 已移除） | ✓ |
| SF02 | 右上角连接状态 | 数秒内显示 `open`（绿色） | |
| SF03 | 页面 Cookie 条 | 显示"⚠️ 尚未配置 Cookie，请先获取" | |
| SF04 | 设置面板 | 显示默认下载目录（末尾 `data/downloads`） | |
| SF05 | curl `GET /api/health` | `{"app":"BiliDownloader","status":"ok","version":"0.1.1"}` | |
| SF06 | 刷新页面 | 任务列表/设置/Cookie 状态保持（非 404） | |

## 2. URL 解析与任务创建（U）

| # | 操作 | 预期 | 结果 |
|---|------|------|------|
| U01 | 输入三条示例链接（各一行）提交 | 创建 3 个任务；每行标签显示**标准链接/裸BV号** | |
| U02 | 输入 `https://b23.tv/xxx` 短链 | 行标签显示**短链**；提交后任务创建成功（解析需网络） | |
| U03 | 标准链接加 `?p=2` | 标签仍为**标准链接**；任务创建成功 | |
| U04 | 提交 `https://example.com/x` | 行标签显示**不支持**，任务不创建 | |
| U05 | 直接 curl POST 非法 URL | `422` + `{"code":"UNSUPPORTED_URL",...}` | |
| U06 | 空 textarea 点提交 | 按钮禁用（0 个有效）或提示"没有可识别的链接" | |
| U07 | 批量 10 个合法 URL | 全部创建；列表计数 +10 | |
| U08 | 批量 11+ 个 URL（含空行干扰） | 行内红字"最多 10 条（当前 N）"；提交按钮禁用（P2） | ✓ |
| U09 | curl POST 11 条 | `422` + `{"code":"BATCH_TOO_LARGE",...}`；恰好 10 条 201（P2） | ✓ |

## 3. 设置（S）

| # | 操作 | 预期 | 结果 |
|---|------|------|------|
| S01 | 设置 → 修改 → 输入 `/tmp/bili_test` → 保存 | 提示"已保存"；目录被创建 | |
| S02 | 刷新页面 | 仍显示 `/tmp/bili_test`（持久化生效） | |
| S03 | 输入 `relative/path` 保存 | 红字"输出目录必须是绝对路径" | |
| S04 | 输入已存在**文件**路径（如 `~/Downloads/某个文件`）保存 | 红字"路径已存在但不是目录" | |
| S05 | 输入 `/System/xxx` 保存 | 红字"无法创建输出目录/不可写"类提示 | |
| S06 | 若在 pywebview 环境 | "选择目录…"调用系统原生目录选择器（M6 才可实测，浏览器模式跳过） | |
| S07 | 改回原默认目录并保存 | 恢复默认；清理 `/tmp/bili_test` | |

## 4. Cookie（C）

| # | 操作 | 预期 | 结果 |
|---|------|------|------|
| C01 | 点击"获取 Cookie" | 新窗口打开 bilibili.com；页面出现书签引导框 | |
| C02 | 点击"复制书签脚本" | 提示"已复制 ✓" | |
| C03 | 浏览器新建书签：地址粘贴脚本（地址以 `javascript:` 开头） | 书签创建成功 | |
| C04 | 在**已登录**的 bilibili 页面点击该书签 | 弹窗"Cookie 已保存，登录用户：xxx" | |
| C05 | 回到应用页 | Cookie 条变绿显示用户名；或点"刷新状态"后变绿 | |
| C06 | 粘贴兜底：F12 → Network 复制 Cookie → 粘贴提交 | 校验通过 → 保存成功（效果同 C04） | |
| C07 | 提交伪造 Cookie（如 `SESSDATA=xxx`） | 红字"未登录或 Cookie 已失效，请重新获取" | |
| C08 | curl `GET /api/cookie/status` | `valid:true` + `uname`（C05 之后） | |
| C09 | 打包版：点"获取 Cookie"（P5） | 弹出**内置浏览器**窗口打开 bilibili.com；登录后自动捕获保存；guide 返回无感文案（无"请使用手动粘贴方式"） | ✓ |

> 注意：书签回传固定 `127.0.0.1:8000`，测试时后端必须在该端口（仅开发模式；打包版动态端口 bookmarlet 不适用）。

## 5. 下载链路（D，需有效 Cookie）

| # | 操作 | 预期 | 结果 |
|---|------|------|------|
| D01 | 提交 `BV1JRuA6vEvd` | 条形码进度条：蓝（下载滚动）→ 琥珀（转码）→ 绿（100%）；面板状态同步 | ✓（双平台） |
| D02 | 任务完成后 | "下载"按钮出现；输出目录存在 `<标题>.mp3`（中文文件名正常） | ✓（双平台） |
| D03 | 点击"下载" | 浏览器下载文件，文件名含中文且**无乱码** | |
| D04 | 再次下载同一链接 | 产生 `标题 (1).mp3`（不覆盖） | |
| D05 | 下载进行中（大视频更佳）点"取消" | 任务变"已取消"；无残留转码进程（`ps aux | grep ffmpeg` 观察） | |
| D06 | 设置改目录后创建新任务 | 成品落在新目录（S 用例已说明） | |
| D07 | 快速连续提交 5 个任务 | **串行执行**：同一时刻仅 1 个在下载 | |

## 6. WS 实时性（W）

| # | 操作 | 预期 | 结果 |
|---|------|------|------|
| W01 | 下载时观察控制台 WS 消息 | 事件类型依次 `task.created → task.progress（≤500ms 间隔）→ task.phase → task.done` | |
| W02 | 任务完成前，`Ctrl+C` 杀掉后端再重启 | 进行中任务变**中断**（灰徽标）进历史；已终态任务原样恢复（P3）；前端 WS 自动重连后快照可见 | ✓ |
| W03 | 进度期间页面不卡顿 | 滚动/拖拽场景流畅，无事件风暴 | |

## 7. 3D 可视化（V）

> **已作废**：v0.1.0 需求变更移除 three.js（纯白主题），本节约束改为"页面无 3D 残留、交互不卡顿"。

| # | 操作 | 预期 | 结果 |
|---|------|------|------|
| V01 | 创建多个任务并滚动列表 | 页面流畅无卡顿、无 3D 场景残留 | |

## 8. 错误与边界（E）

| # | 操作 | 预期 | 结果 |
|---|------|------|------|
| E01 | 无 Cookie 提交任务（先清空 Cookie 文件） | 任务快速变红 `failed`；原因"Cookie 失效，请重新获取"；CookieBar 出现**联动横幅**"立即重新获取" | |
| E02 | 输出目录设为只读（`chmod 555`）后下载 | `failed(path)` + 建议"在设置中修改下载目录" | |
| E03 | 断网后提交任务 | `failed(network)` + 建议"检查网络" | |
| E04 | 关闭后端后打开前端 | 页面不崩溃；WS 状态 closed 并在后端恢复后自动重连 | |
| E05 | 表单提交瞬间连点两次 | 按钮禁用中，不产生重复创建 | |
| E06 | 提交 11 条 URL（curl 绕过前端） | `422 BATCH_TOO_LARGE`，无任务创建（P2） | ✓ |

## 9. 性能与资源（P）

| # | 操作 | 预期 | 结果 |
|---|------|------|------|
| P01 | 一个较大视频下载全程观察（DevTools → Memory → 堆快照对比前后） | 无持续内存增长（three 场景不泄漏） | |
| P02 | 10+ 任务时界面操作 | 交互无明显卡顿 | |

---

## 10. 历史持久化与重试（H，P3/P4）

| # | 操作 | 预期 | 结果 |
|---|------|------|------|
| H01 | 完成一个成功 + 一个失败任务 | `data/tasks.json` 出现两条终态记录（含 created_at/finished_at/error 字段） | ✓ |
| H02 | 重启后端再打开页面 | 两任务在**历史**分组恢复（done 行可"下载"，failed 行显示失败原因） | ✓ |
| H03 | 下载中重启后端 | 该任务变**中断**（灰徽标）并归入历史（P3） | ✓ |
| H04 | 历史 failed/interrupted/canceled 行点"重试" | 新任务进入"进行中"并执行；按钮短暂"已重试" | ✓ |
| H05 | 连续制造 501+ 条历史（脚本循环） | tasks.json 自动裁剪至最新 500 条 | ✓（自动化用例覆盖） |
| H06 | 手动损坏 tasks.json（写入乱码）后启动 | 按空历史启动，应用不崩溃（P3 容错） | ✓（自动化用例覆盖） |

## 11. 浏览器扩展（X，v1）

> 前置：§0.5 准备完成；测试浏览器 Chrome/Edge 均可；下载为 m4a 直存（不转码）。
> 设计：`docs/design-extension-v1.md`（权限/接口/流程）；验收清单见其 §11。

| # | 操作 | 预期 | 结果 |
|---|------|------|------|
| X01 | 打开**单P**视频页，单击扩展图标 | **立即开始下载**（不弹窗），完成后系统通知；下载目录出现 `标题.m4a`（中文标题正常） | |
| X02 | 打开**多P**视频页，单击扩展图标 | 弹出 popup，列出全部分 P（名称+时长），**默认勾选当前播放 P** | |
| X03 | popup 勾选 P1 + P3 后点下载 | 依次下载生成 `标题-P1.m4a`、`标题-P3.m4a`；popup 关闭不中断 | |
| X04 | 多P 页面按快捷键（默认 Ctrl/Command+Shift+D） | **立即下载当前播放 P**（不进 popup） | |
| X05 | `chrome://extensions/shortcuts` 修改快捷键 | 新快捷键生效（可配置性） | |
| X06 | 未登录（无 SESSDATA）：单P 单击图标 | 弹出确认小窗「未登录，将下载低音质」→ 点继续 → 正常下载 | |
| X07 | 未登录：多P 单击图标 | popup 内显示未登录提示与确认按钮；确认后按勾选下载 | |
| X08 | 同一视频分别用已登录/未登录下载 | 已登录文件码率/体积更高（服务端按登录态给档位） | |
| X09 | 重复下载同名视频 | 浏览器自动重名为 `标题 (1).m4a`，不覆盖（uniquify） | |
| X10 | 标题含 `\/:*?"<>|` 的视频 | 文件名中这些字符被清理（替换为 `_`） | |
| X11 | 非视频页（首页/搜索页/站外）单击图标 | 系统通知「仅支持 bilibili.com/video/ 视频页」 | |
| X12 | 断网或触发 412 时下载 | 重试后仍失败 → 明确失败通知（风控/网络分类），扩展不崩溃 | |

## 12. 回归清单（快速全量）

- [ ] SF01–SF06 冒烟
- [ ] U01–U09 解析与创建（含 P2 批量上限）
- [ ] S01–S07 设置
- [ ] C01–C09 Cookie（含 P5 打包版无感）
- [ ] D01–D07 下载（有 Cookie）
- [ ] H01–H06 历史与重试（P3/P4）
- [ ] W01–W03 WS
- [ ] V01（3D 已移除，仅无残留检查）
- [ ] E01–E06 错误边界
- [ ] P01–P02 性能
- [ ] X01–X12 浏览器扩展（§11，需先完成 0.5 准备）
- [ ] `cd backend && ../venv/bin/python -m pytest tests/` → 全绿（自动用例 96 个）
- [ ] `cd backend && ../venv/bin/ruff check app tests` → All checks passed
- [ ] `cd extension && npm test` → 全绿（wbi/url/naming 纯函数）

## 13. 问题记录模板

```
日期：____
用例：____  结果：____
现象描述：____
复现步骤：____
期望 vs 实际：____
是否阻塞：____
```

> 提示：任何与预期不符的行为，先对照 `docs/design-analysis.md` 核实是设计如此还是缺陷；确认为缺陷后按 `docs/development-guide.md` 的规范修复。